{
	"name": "@kebab-case/ls-manager",
	"version": "1.1.5",
	"author": "kebab-case",
	"repository": {
		"type": "git",
		"url": "https://github.com/flazzarotto/ls-manager.git"
	},
	"license": "GPL-3.0-or-later",
	"bugs": {
		"url": "https://github.com/flazzarotto/ls-manager/issues"
	},
	"homepage": "https://github.com/flazzarotto/ls-manager",
	"keywords": [
		"localStorage",
		"manager",
		"stringify",
		"parse",
		"json",
		"object"
	],
	"scripts": {
		"build": "rm -rf dist && babel src -d dist && chmod +x ./dist/*.js"
	},
	"devDependencies": {
		"@babel/cli": "^7.12.8",
		"@babel/core": "^7.12.9",
		"@babel/preset-env": "^7.12.7",
		"babel-eslint": "^10.1.0"
	},
	"main": "./dist/ls-manager.js",
	"module": "./src/ls-manager.js",
	"description": "This tool allow to simply get and set objects, arrays etc. directly into the window.localStorage, without worrying about parsing / stringifying data.",
	"private": false,
	"eslintConfig": {
		"root": true,
		"env": {
			"node": true
		},
		"extends": [
			"eslint:recommended"
		],
		"parserOptions": {
			"parser": "babel-eslint"
		},
		"rules": {}
	}
}